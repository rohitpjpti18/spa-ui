import { createAction } from "@ngrx/store";

export const pathfinder = createAction('[Path Finder Menu] Paht Finder Menu')
export const sortingVisualiser = createAction('[Sorting menu] Sorting Menu')
export const reset = createAction('[Reset] Reset')